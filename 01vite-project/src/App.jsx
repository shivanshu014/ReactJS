import Chai from "./chai";

function App() {
  return (
    <> 
    <Chai/>
    <h1>Hello World!</h1>
    </> 
  );
}
// fragment <></> is used for return multiple element or component becz react js allowed only one component
// fragments are used to group a list of children without adding extra nodes to the DOM
// react is a library for building user interfaces
export default App;
