function Chai(){
    return(
        <h2>chai in react</h2>
    )
}
export default Chai

// npm - node package manager used for installing the library
// npx - node package executor no need to install just directly used it
//  create react-app is bulky and time taking
//  use vite instead
//  vite is a build tool that aims to provide a faster and leaner development experience for modern web projects.